# Bootstrap the ordered zsh config fragments.
_zshrc_dir="${${(%):-%N}:A:h}"

source "${_zshrc_dir}/rc"

# Machine-specific overrides stay last.
[[ -f "${HOME}/.zsh.local" ]] && source "${HOME}/.zsh.local"

unset _zshrc_dir
