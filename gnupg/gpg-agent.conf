# Enable SSH support
enable-ssh-support

# Cache TTL (seconds)
default-cache-ttl 60
max-cache-ttl 120

# Pinentry program (macOS)
pinentry-program /opt/homebrew/bin/pinentry-mac

# Verbose logging
verbose
